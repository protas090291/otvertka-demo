# 🚀 Сохранение проекта на GitHub

## 📋 Пошаговая инструкция

### 1. Создание репозитория на GitHub

1. **Откройте [GitHub.com](https://github.com)** в браузере
2. **Войдите в свой аккаунт** (или создайте новый)
3. **Нажмите зеленую кнопку "New"** или "+" → "New repository"
4. **Заполните данные:**
   - Repository name: `otvertka-construction-management` (или любое другое название)
   - Description: `Система управления строительством - Отвёртка`
   - Выберите **Public** или **Private**
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license"
5. **Нажмите "Create repository"**

### 2. Инициализация Git в проекте

Откройте командную строку в папке проекта и выполните:

```bash
# Перейдите в папку проекта
cd C:\Users\User\Downloads\Helper2

# Инициализируйте Git репозиторий
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: Система управления строительством"

# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/otvertka-construction-management.git

# Переименуйте основную ветку в main
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

### 3. Создание .gitignore файла

Создайте файл `.gitignore` в корне проекта:

```gitignore
# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Production builds
dist/
build/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE files
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
env.bak/
venv.bak/

# Backend specific
backend/__pycache__/
backend/*.pyc
backend/documents/*.docx
backend/documents/*.pdf

# Temporary files
*.tmp
*.temp
```

### 4. Обновление README.md

Создайте или обновите файл `README.md`:

```markdown
# 🔧 Отвёртка - Система управления строительством

Современная система управления строительными проектами с поддержкой задач, дефектов, материалов и документооборота.

## ✨ Возможности

- 📋 Управление задачами и проектами
- 🐛 Система отслеживания дефектов
- 📦 Управление материалами и складом
- 📄 Генерация документов
- 👥 Многопользовательская система с ролями
- 📊 Аналитика и отчеты

## 🛠️ Технологии

- **Frontend**: React 18 + TypeScript + Vite
- **Backend**: FastAPI + Python
- **База данных**: Supabase (PostgreSQL)
- **Стилизация**: Tailwind CSS
- **Иконки**: Lucide React

## 🚀 Быстрый старт

### Установка зависимостей

```bash
# Frontend
npm install

# Backend
cd backend
pip install -r requirements.txt
```

### Запуск приложения

```bash
# Запуск frontend (порт 5175)
npm run dev

# Запуск backend (порт 8000)
cd backend
python simple_main.py
```

## 📁 Структура проекта

```
Helper2/
├── src/                    # Frontend код
│   ├── components/         # React компоненты
│   ├── lib/               # API и утилиты
│   └── types/             # TypeScript типы
├── backend/               # Backend код
│   ├── main.py           # Основной FastAPI сервер
│   └── documents/        # Генераторы документов
└── README.md
```

## 👥 Роли пользователей

- **Клиент** - просмотр проектов и отчетов
- **Прораб** - управление задачами и бригадой
- **Подрядчик** - выполнение работ
- **Рабочий** - выполнение задач
- **Кладовщик** - управление материалами
- **Технадзор** - контроль качества

## 📄 Лицензия

MIT License
```

### 5. Команды для работы с Git

```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Сделать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push

# Получить изменения с GitHub
git pull

# Посмотреть историю
git log --oneline
```

## 🔧 Настройка Supabase

1. Создайте проект на [Supabase.com](https://supabase.com)
2. Скопируйте URL и API ключ
3. Создайте файл `.env` в корне проекта:

```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

## ✅ Готово!

Теперь ваш проект сохранен на GitHub и готов к совместной работе!





















