# 📅 Автоматическое обновление срока завершения проекта

## 🔍 Текущая ситуация

**Вопрос:** Обновляется ли срок завершения проекта каждый день автоматически?

**Ответ:** На данный момент **автоматическое обновление срока завершения не реализовано**.

## 📋 Что есть сейчас

1. **Статический срок завершения:**
   - Срок завершения хранится в таблице `projects` в поле `end_date`
   - Обновляется только вручную через SQL скрипт `update_project_end_date.sql`
   - Или через интерфейс редактирования проекта

2. **Расчет времени:**
   - Система рассчитывает процент прошедшего времени (`timeProgress`)
   - Сравнивает фактический прогресс с плановым
   - Показывает оставшиеся дни до завершения

3. **Нет автоматического пересчета:**
   - Срок завершения **не пересчитывается** автоматически на основе:
     - Фактического прогресса работ
     - Задержек в выполнении задач
     - Изменений в планах

## 💡 Как это должно работать

### Вариант 1: Пересчет на основе эффективности

Если фактический прогресс отстает от планового, срок завершения должен автоматически сдвигаться:

```
Новый срок = Текущая дата + (Оставшиеся дни × Коэффициент задержки)

Коэффициент задержки = Плановый прогресс / Фактический прогресс
```

**Пример:**
- Плановый прогресс: 50%
- Фактический прогресс: 25%
- Коэффициент: 50 / 25 = 2
- Если осталось 100 дней, новый срок: 100 × 2 = 200 дней

### Вариант 2: Пересчет на основе задержек задач

Если есть задачи со статусом `delayed`, срок завершения сдвигается:

```
Новый срок = Плановый срок + (Сумма задержек по всем задачам)
```

### Вариант 3: Ежедневный пересчет

Каждый день система автоматически:
1. Проверяет фактический прогресс
2. Сравнивает с плановым
3. Пересчитывает срок завершения
4. Обновляет `end_date` в базе данных

## 🛠️ Реализация

### Шаг 1: Создать функцию пересчета

**Файл:** `Helper2/src/lib/projectsApi.ts`

```typescript
/**
 * Автоматически пересчитать срок завершения проекта на основе фактического прогресса
 */
export const recalculateProjectEndDate = async (projectId: string): Promise<Date | null> => {
  try {
    // Получаем проект
    const project = await getProjectById(projectId);
    if (!project) return null;

    // Получаем фактический прогресс
    const { getProjectProgress } = await import('./tasksApi');
    const progress = await getProjectProgress(projectId);
    const actualProgress = progress.averageProgress || 0;

    // Получаем даты проекта
    const startDate = project.start_date ? new Date(project.start_date) : null;
    const plannedEndDate = project.end_date ? new Date(project.end_date) : null;
    
    if (!startDate || !plannedEndDate) return null;

    // Рассчитываем плановый прогресс (сколько должно быть выполнено к сегодняшнему дню)
    const currentDate = new Date();
    const totalPlannedDays = Math.ceil((plannedEndDate.getTime() - startDate.getTime()) / (1000 * 60 * 60 * 24));
    const daysFromStart = Math.ceil((currentDate.getTime() - startDate.getTime()) / (1000 * 60 * 60 * 24));
    const plannedProgress = totalPlannedDays > 0 ? (daysFromStart / totalPlannedDays) * 100 : 0;

    // Если фактический прогресс меньше планового, сдвигаем срок
    if (actualProgress < plannedProgress && actualProgress > 0) {
      const delayCoefficient = plannedProgress / actualProgress;
      const remainingDays = Math.ceil((plannedEndDate.getTime() - currentDate.getTime()) / (1000 * 60 * 60 * 24));
      const newRemainingDays = Math.ceil(remainingDays * delayCoefficient);
      const newEndDate = new Date(currentDate);
      newEndDate.setDate(newEndDate.getDate() + newRemainingDays);
      
      // Обновляем срок завершения в базе данных
      await updateProject(projectId, {
        end_date: newEndDate.toISOString().split('T')[0]
      });
      
      return newEndDate;
    }

    return plannedEndDate;
  } catch (error) {
    console.error('Ошибка пересчета срока завершения:', error);
    return null;
  }
};
```

### Шаг 2: Создать cron job или scheduled task

**Вариант А: Backend cron job (Python)**

**Файл:** `Helper2/backend/update_project_dates.py`

```python
import os
import schedule
import time
from supabase import create_client, Client

SUPABASE_URL = os.getenv("SUPABASE_URL")
SUPABASE_KEY = os.getenv("SUPABASE_SERVICE_ROLE_KEY")

def update_project_end_dates():
    """Ежедневное обновление сроков завершения проектов"""
    supabase: Client = create_client(SUPABASE_URL, SUPABASE_KEY)
    
    # Получаем все активные проекты
    projects = supabase.table("projects").select("*").eq("status", "construction").execute()
    
    for project in projects.data:
        # Вызываем API для пересчета срока
        # (логика пересчета должна быть в API endpoint)
        pass

# Запускаем каждый день в 00:00
schedule.every().day.at("00:00").do(update_project_end_dates)

while True:
    schedule.run_pending()
    time.sleep(60)
```

**Вариант Б: Supabase Edge Function (рекомендуется)**

Создать Edge Function в Supabase, которая будет запускаться по расписанию.

**Вариант В: Frontend при загрузке проекта**

Пересчитывать срок при каждом открытии карточки проекта (менее надежно).

### Шаг 3: Добавить API endpoint

**Файл:** `Helper2/backend/simple_main.py`

```python
@app.post("/api/projects/{project_id}/recalculate-end-date")
async def recalculate_project_end_date(project_id: str):
    """Пересчитать срок завершения проекта на основе фактического прогресса"""
    # Логика пересчета
    pass
```

## 📊 Логика пересчета

### Формула пересчета:

```
1. Рассчитываем плановый прогресс:
   plannedProgress = (daysFromStart / totalPlannedDays) × 100

2. Получаем фактический прогресс:
   actualProgress = средний прогресс из progress_data

3. Если actualProgress < plannedProgress:
   delayCoefficient = plannedProgress / actualProgress
   newRemainingDays = remainingDays × delayCoefficient
   newEndDate = currentDate + newRemainingDays

4. Обновляем end_date в базе данных
```

## ⚙️ Настройка

### Включить автоматическое обновление:

1. **Создать функцию пересчета** (см. выше)
2. **Настроить cron job** или scheduled task
3. **Добавить настройку** в интерфейсе (включить/выключить автоматический пересчет)

### Параметры:

- **Частота обновления:** ежедневно, еженедельно, по требованию
- **Минимальная задержка:** обновлять только если задержка > X%
- **Максимальный сдвиг:** не сдвигать более чем на Y дней

## ✅ Рекомендации

1. **Реализовать автоматический пересчет** на основе фактического прогресса
2. **Добавить настройку** для включения/выключения автоматического обновления
3. **Логировать изменения** срока завершения для отслеживания
4. **Уведомлять пользователей** о сдвиге срока завершения

## 🚀 Быстрое решение

Если нужно быстро реализовать:

1. Создать функцию `recalculateProjectEndDate` в `projectsApi.ts`
2. Вызывать её при загрузке проекта в `ProjectCard.tsx`
3. Позже добавить cron job для ежедневного обновления

---

**Текущий статус:** ❌ Автоматическое обновление срока завершения **не реализовано**

**Рекомендация:** ✅ Реализовать автоматический пересчет на основе фактического прогресса










