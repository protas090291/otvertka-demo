# Инструкция по настройке интеграции с Яндекс Диском

## Описание

Интеграция позволяет просматривать и скачивать файлы из определенной папки на Яндекс Диске. Файлы обновляются при открытии раздела "Документы из Яндекс Диска".

## Требования

1. OAuth токен Яндекс Диска
2. Доступ к папке на Яндекс Диске (может быть общий доступ)
3. Путь к папке на Яндекс Диске

## Получение OAuth токена

**📖 Подробная инструкция:** См. файл `КАК_ПОЛУЧИТЬ_ТОКЕН_ЯНДЕКС_ДИСК.md` для пошаговой инструкции с примерами.

### Краткая версия:

1. Перейдите на [Яндекс ID для разработчиков](https://oauth.yandex.ru/)
2. Создайте новое приложение (Web-сервисы, Callback URI: `http://localhost:8000`)
3. Скопируйте Client ID
4. Откройте в браузере: `https://oauth.yandex.ru/authorize?response_type=token&client_id=YOUR_CLIENT_ID`
5. Разрешите доступ и скопируйте токен из URL (параметр `access_token`)

## Настройка переменных окружения

### Backend

Создайте файл `.env` в папке `Helper2/backend/`:

```env
YANDEX_DISK_TOKEN=ваш_oauth_токен_здесь
YANDEX_DISK_FOLDER_PATH=/путь/к/папке
```

**Пример:**
```env
YANDEX_DISK_TOKEN=AQAAAAAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
YANDEX_DISK_FOLDER_PATH=/Документы/Строительство
```

### Важные замечания

1. **Путь к папке**: 
   - Если папка в корне: `/Название_папки`
   - Если вложенная: `/Папка1/Папка2/Название_папки`
   - Для корня диска: `/` (не рекомендуется)

2. **Общий доступ**: 
   - Если вам дали доступ к папке (не ваш Яндекс Диск), используйте токен вашего аккаунта
   - Убедитесь, что у вас есть права на чтение папки

3. **Безопасность**:
   - НЕ коммитьте файл `.env` в Git
   - Храните токен в безопасном месте
   - Токен имеет срок действия, при необходимости обновите его

## Проверка настройки

### 1. Проверка токена

Выполните в терминале:

```bash
cd Helper2/backend
python -c "from yandex_disk_api import get_yandex_disk_token; print('Токен установлен:', bool(get_yandex_disk_token()))"
```

### 2. Проверка доступа к папке

Запустите backend и проверьте endpoint:

```bash
curl http://localhost:8000/api/yandex-disk/files
```

Если все настроено правильно, вы получите список файлов.

## Использование

1. Запустите backend сервер:
```bash
cd Helper2/backend
python simple_main.py
```

2. Откройте приложение в браузере
3. Перейдите в раздел "Документы из Яндекс Диска" в меню
4. Файлы автоматически загрузятся при открытии раздела
5. Используйте кнопку "Обновить" для обновления списка файлов
6. Нажмите "Скачать" для скачивания файла

## Функции

- ✅ Просмотр списка файлов из папки
- ✅ Фильтрация по типу (файлы/папки)
- ✅ Поиск по имени файла
- ✅ Скачивание файлов
- ✅ Обновление списка файлов
- ✅ Отображение метаданных (размер, дата изменения)

## Ограничения

- ⚠️ Только чтение и скачивание (загрузка файлов не поддерживается)
- ⚠️ Работает только с одной папкой (настроенной в переменных окружения)
- ⚠️ Требуется OAuth токен для доступа

## Устранение неполадок

### Ошибка "YANDEX_DISK_TOKEN не установлен"

- Проверьте, что файл `.env` создан в папке `backend/`
- Убедитесь, что переменная `YANDEX_DISK_TOKEN` указана правильно
- Перезапустите backend сервер

### Ошибка "Неверный OAuth токен"

- Токен мог истечь, получите новый токен
- Проверьте правильность токена в `.env`

### Ошибка "Нет доступа к указанной папке"

- Проверьте путь к папке в `YANDEX_DISK_FOLDER_PATH`
- Убедитесь, что у вас есть права на чтение папки
- Проверьте, что папка существует на Яндекс Диске

### Ошибка "Папка не найдена"

- Проверьте правильность пути в `YANDEX_DISK_FOLDER_PATH`
- Путь должен начинаться с `/`
- Убедитесь, что папка существует

## Дополнительная информация

- [Документация Яндекс Диск API](https://yandex.ru/dev/disk/api/doc/ru/)
- [OAuth Яндекс ID](https://yandex.ru/dev/id/doc/ru/)

