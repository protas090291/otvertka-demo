# 🔧 Решение проблемы: данные не отображаются в разделах "Бюджет" и "Материалы"

## 🔍 Возможные причины

### 1. Таблицы не существуют в Supabase
- Таблицы `budget_items` и `warehouse_items` не созданы
- Решение: Выполните SQL скрипты для создания таблиц

### 2. Таблицы пусты (нет данных)
- Таблицы существуют, но в них нет записей
- Решение: Добавьте данные через приложение или SQL скрипты

### 3. Проблема с фильтрацией (Бюджет)
- Данные есть, но фильтруются по неправильному `project_id`
- Решение: Проверьте `project_id` в данных и выберите правильный проект

### 4. Проблема с RLS политиками
- RLS блокирует доступ к данным
- Решение: Проверьте и настройте RLS политики

### 5. Ошибки в API запросах
- Ошибки при загрузке данных из Supabase
- Решение: Проверьте консоль браузера на наличие ошибок

---

## 🛠️ Пошаговая диагностика

### Шаг 1: Откройте диагностический файл

1. Откройте файл `ПРОВЕРКА_БЮДЖЕТ_И_МАТЕРИАЛЫ.html` в браузере
2. Введите ваши Supabase credentials:
   - **Supabase URL**: `https://xxxxx.supabase.co`
   - **Supabase Anon Key**: `eyJhbGc...`
3. Нажмите **"Инициализировать подключение"**

### Шаг 2: Проверьте наличие таблиц

1. Нажмите **"Проверить наличие таблиц"**
2. Если таблицы не существуют:
   - ✅ Выполните SQL скрипт `supabase-budget-setup.sql` (для бюджета)
   - ✅ Выполните SQL скрипт `supabase-materials-setup.sql` (для материалов)
   - ✅ Или выполните `supabase-complete-setup.sql` (для всего сразу)

### Шаг 3: Проверьте данные в таблицах

1. Нажмите **"Проверить данные бюджета"**
2. Нажмите **"Проверить данные материалов"**
3. Если таблицы пусты:
   - ✅ Добавьте данные через приложение
   - ✅ Или выполните SQL скрипты с тестовыми данными

### Шаг 4: Проверьте консоль браузера

1. Откройте приложение в браузере
2. Нажмите `F12` для открытия DevTools
3. Перейдите на вкладку **Console**
4. Откройте раздел **"Бюджет"** или **"Материалы"**
5. Проверьте наличие ошибок:
   - ❌ `PGRST116` - таблица не существует
   - ❌ `row-level security` - проблема с RLS
   - ❌ `invalid input syntax` - проблема с форматом данных

---

## 🔧 Решения по проблемам

### Проблема 1: Таблицы не существуют

**Симптомы:**
- Ошибка в консоли: `PGRST116` или `does not exist`
- Диагностический файл показывает: "Таблица НЕ СУЩЕСТВУЕТ"

**Решение:**

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Создайте новый запрос
5. Скопируйте и выполните один из SQL скриптов:
   - `Helper2/supabase-budget-setup.sql` (только бюджет)
   - `Helper2/supabase-materials-setup.sql` (только материалы)
   - `Helper2/supabase-complete-setup.sql` (все таблицы)
6. Нажмите **Run** (Выполнить)

### Проблема 2: Таблицы пусты

**Симптомы:**
- Таблицы существуют, но нет данных
- Разделы показывают пустые списки

**Решение:**

**Вариант А: Добавить данные через приложение**

1. Откройте раздел **"Бюджет"**
2. Нажмите **"Добавить операцию"** (для подрядчика)
3. Заполните форму и сохраните
4. Проверьте, что данные появились

**Вариант Б: Добавить тестовые данные через SQL**

1. Откройте SQL Editor в Supabase
2. Выполните SQL скрипты с тестовыми данными:
   - `supabase-budget-setup.sql` (содержит тестовые данные)
   - `supabase-materials-setup.sql` (содержит тестовые данные)

### Проблема 3: Фильтрация по project_id (Бюджет)

**Симптомы:**
- Данные есть в таблице, но не отображаются
- В консоли нет ошибок

**Решение:**

1. Откройте консоль браузера (`F12`)
2. Перейдите на вкладку **Console**
3. Откройте раздел **"Бюджет"**
4. Введите в консоли:
   ```javascript
   // Проверьте, какие project_id есть в данных
   // Это нужно сделать в компоненте BudgetViewWithAPI
   ```
5. Проверьте, какой `project_id` используется в данных
6. Убедитесь, что выбран правильный проект в интерфейсе

**Или исправьте код:**

В файле `Helper2/src/components/BudgetViewWithAPI.tsx` найдите строку:
```typescript
const [selectedProject, setSelectedProject] = useState('1');
```

Измените на:
```typescript
const [selectedProject, setSelectedProject] = useState(''); // Показывать все проекты
```

Или добавьте логику для автоматического выбора проекта из данных.

### Проблема 4: RLS блокирует доступ

**Симптомы:**
- Ошибка в консоли: `row-level security` или `permission denied`
- Диагностический файл показывает проблему с RLS

**Решение:**

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Authentication** → **Policies**
4. Найдите таблицы `budget_items` и `warehouse_items`
5. Проверьте политики доступа:
   - Должна быть политика для `SELECT` (чтение)
   - Должна быть политика для `INSERT` (создание)
   - Должна быть политика для `UPDATE` (обновление)
   - Должна быть политика для `DELETE` (удаление)

**Или выполните SQL для создания политик:**

```sql
-- Для budget_items
CREATE POLICY "Enable read access for all users" ON budget_items
    FOR SELECT USING (true);

CREATE POLICY "Enable insert for all users" ON budget_items
    FOR INSERT WITH CHECK (true);

CREATE POLICY "Enable update for all users" ON budget_items
    FOR UPDATE USING (true);

CREATE POLICY "Enable delete for all users" ON budget_items
    FOR DELETE USING (true);

-- Для warehouse_items
CREATE POLICY "Enable read access for all users" ON warehouse_items
    FOR SELECT USING (true);

CREATE POLICY "Enable insert for all users" ON warehouse_items
    FOR INSERT WITH CHECK (true);

CREATE POLICY "Enable update for all users" ON warehouse_items
    FOR UPDATE USING (true);

CREATE POLICY "Enable delete for all users" ON warehouse_items
    FOR DELETE USING (true);
```

### Проблема 5: Ошибки в API запросах

**Симптомы:**
- Ошибки в консоли браузера
- Данные не загружаются

**Решение:**

1. Откройте консоль браузера (`F12`)
2. Перейдите на вкладку **Network**
3. Откройте раздел **"Бюджет"** или **"Материалы"**
4. Найдите запросы к Supabase
5. Проверьте статус ответа:
   - `200` - успешно
   - `400` - ошибка запроса
   - `401` - проблема с аутентификацией
   - `403` - проблема с правами доступа
   - `404` - таблица не найдена

**Проверьте переменные окружения:**

1. Убедитесь, что файл `.env` содержит:
   ```env
   VITE_SUPABASE_URL=https://xxxxx.supabase.co
   VITE_SUPABASE_ANON_KEY=eyJhbGc...
   ```
2. Перезапустите frontend сервер после изменения `.env`

---

## 📋 Чек-лист проверки

- [ ] Таблицы `budget_items` и `warehouse_items` существуют в Supabase
- [ ] В таблицах есть данные (хотя бы одна запись)
- [ ] RLS политики настроены правильно
- [ ] Переменные окружения `.env` настроены правильно
- [ ] Нет ошибок в консоли браузера
- [ ] Frontend сервер перезапущен после изменения `.env`
- [ ] Выбран правильный проект в разделе "Бюджет" (если используется фильтрация)

---

## 🚀 Быстрое решение

Если вы не уверены, в чем проблема, выполните следующие шаги:

1. **Создайте таблицы** (если их нет):
   ```sql
   -- Выполните в Supabase SQL Editor
   -- Файл: supabase-complete-setup.sql
   ```

2. **Добавьте тестовые данные**:
   ```sql
   -- Выполните в Supabase SQL Editor
   -- Файл: supabase-budget-setup.sql (содержит тестовые данные)
   -- Файл: supabase-materials-setup.sql (содержит тестовые данные)
   ```

3. **Проверьте в приложении**:
   - Откройте раздел **"Бюджет"**
   - Откройте раздел **"Материалы"**
   - Данные должны отображаться

4. **Если данные не отображаются**:
   - Откройте консоль браузера (`F12`)
   - Проверьте ошибки
   - Используйте диагностический файл `ПРОВЕРКА_БЮДЖЕТ_И_МАТЕРИАЛЫ.html`

---

## 📞 Дополнительная помощь

Если проблема не решена:

1. **Соберите информацию:**
   - Скриншот ошибки из консоли браузера
   - Результаты диагностического файла
   - SQL запросы, которые вы выполняли

2. **Проверьте логи:**
   - Консоль браузера (F12)
   - Network вкладка (для проверки запросов к Supabase)
   - Supabase Dashboard → Logs

3. **Проверьте документацию:**
   - `АНАЛИЗ_РАЗДЕЛОВ_БЮДЖЕТ_И_МАТЕРИАЛЫ.md`
   - `DATABASE_INTEGRATION_GUIDE.md`

---

**Готово!** Теперь вы знаете, как диагностировать и решить проблему с отображением данных. 🎉










