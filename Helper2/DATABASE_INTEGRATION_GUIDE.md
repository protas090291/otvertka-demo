# 🗄️ Руководство по интеграции с базой данных

## 📋 Обзор

Это руководство поможет вам настроить приложение "Отвёртка" для работы с реальными данными из базы данных Supabase вместо моковых данных.

## ✅ Что уже готово

### API модули созданы:
- ✅ `budgetApi.ts` - API для работы с бюджетом
- ✅ `materialsApi.ts` - API для работы с материалами/складом  
- ✅ `projectsApi.ts` - API для работы с проектами
- ✅ `reportsApi.ts` - API для работы с отчетами
- ✅ `scheduleApi.ts` - API для работы с расписанием

### SQL скрипты созданы:
- ✅ `supabase-complete-setup.sql` - Полная настройка всех таблиц
- ✅ `supabase-budget-setup.sql` - Настройка таблиц бюджета
- ✅ `supabase-materials-setup.sql` - Настройка таблиц материалов
- ✅ `supabase-reports-setup.sql` - Настройка таблиц отчетов
- ✅ `supabase-schedule-setup.sql` - Настройка таблиц расписания

## 🚀 Пошаговая настройка

### Шаг 1: Настройка базы данных

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Создайте новый запрос
5. Скопируйте и выполните содержимое файла `supabase-tables-only.sql` (только таблицы без тестовых данных)

**Альтернативно:** Если нужны тестовые данные, используйте `supabase-complete-setup.sql`

### Шаг 2: Проверка таблиц

После выполнения SQL скрипта убедитесь, что созданы следующие таблицы:

- `budget_items` - Бюджетные операции
- `warehouse_items` - Складские позиции
- `reports` - Отчеты
- `schedule_events` - События расписания

ℹ️ **Таблица `projects` не создается** - уже существует в системе

### Шаг 3: Обновление компонентов

Теперь нужно обновить компоненты для использования новых API:

#### 3.1 Обновить BudgetView
```typescript
// Заменить импорт
import { getAllBudgetItems, getProjectBudget, createBudgetItem } from '../lib/budgetApi';

// Заменить моковые данные на реальные API вызовы
const [budgetItems, setBudgetItems] = useState<BudgetItem[]>([]);

useEffect(() => {
  const loadBudgetData = async () => {
    const items = await getAllBudgetItems();
    setBudgetItems(items);
  };
  loadBudgetData();
}, []);
```

#### 3.2 Обновить MaterialsView
```typescript
// Заменить импорт
import { getAllWarehouseItems, updateWarehouseItemQuantity } from '../lib/materialsApi';

// Заменить моковые данные на реальные API вызовы
const [warehouseItems, setWarehouseItems] = useState<WarehouseItem[]>([]);

useEffect(() => {
  const loadMaterialsData = async () => {
    const items = await getAllWarehouseItems();
    setWarehouseItems(items);
  };
  loadMaterialsData();
}, []);
```

#### 3.3 Обновить ProjectsView
```typescript
// Заменить импорт
import { getAllProjects, createProject } from '../lib/projectsApi';

// Заменить моковые данные на реальные API вызовы
const [projects, setProjects] = useState<Project[]>([]);

useEffect(() => {
  const loadProjectsData = async () => {
    const projectsData = await getAllProjects();
    setProjects(projectsData);
  };
  loadProjectsData();
}, []);
```

#### 3.4 Обновить ReportsView
```typescript
// Заменить импорт
import { getAllReports, createReport } from '../lib/reportsApi';

// Заменить моковые данные на реальные API вызовы
const [reports, setReports] = useState<Report[]>([]);

useEffect(() => {
  const loadReportsData = async () => {
    const reportsData = await getAllReports();
    setReports(reportsData);
  };
  loadReportsData();
}, []);
```

#### 3.5 Обновить ScheduleView
```typescript
// Заменить импорт
import { getAllScheduleEvents, createScheduleEvent } from '../lib/scheduleApi';

// Заменить моковые данные на реальные API вызовы
const [events, setEvents] = useState<ScheduleEvent[]>([]);

useEffect(() => {
  const loadScheduleData = async () => {
    const eventsData = await getAllScheduleEvents();
    setEvents(eventsData);
  };
  loadScheduleData();
}, []);
```

## 📊 Структура базы данных

### Таблица `projects`
- `id` - Уникальный идентификатор проекта
- `name` - Название проекта
- `description` - Описание проекта
- `status` - Статус (planning, construction, completed, on-hold, cancelled)
- `progress` - Прогресс (0-100%)
- `total_budget` - Общий бюджет
- `spent` - Потраченная сумма

### Таблица `budget_items`
- `id` - Уникальный идентификатор операции
- `category` - Категория операции
- `description` - Описание операции
- `amount` - Сумма
- `type` - Тип (expense/income)
- `project_id` - Ссылка на проект

### Таблица `warehouse_items`
- `id` - Уникальный идентификатор позиции
- `name` - Название позиции
- `category` - Категория (materials, tools, equipment, consumables)
- `quantity` - Количество
- `cost_per_unit` - Цена за единицу
- `status` - Статус (in-stock, low-stock, out-of-stock, reserved)

### Таблица `reports`
- `id` - Уникальный идентификатор отчета
- `title` - Название отчета
- `type` - Тип отчета (work_report, defect_report, etc.)
- `project_id` - Ссылка на проект
- `status` - Статус (draft, pending, approved, rejected)

### Таблица `schedule_events`
- `id` - Уникальный идентификатор события
- `title` - Название события
- `type` - Тип события (meeting, task, deadline, etc.)
- `start_date` - Дата начала
- `status` - Статус (scheduled, in-progress, completed, etc.)

## 🔧 Функции API

### BudgetApi
- `getAllBudgetItems()` - Получить все бюджетные операции
- `getProjectBudget(projectId)` - Получить бюджет проекта
- `createBudgetItem(item)` - Создать новую операцию
- `updateBudgetItem(id, updates)` - Обновить операцию
- `deleteBudgetItem(id)` - Удалить операцию

### MaterialsApi
- `getAllWarehouseItems()` - Получить все позиции склада
- `getWarehouseItemsByCategory(category)` - Получить по категории
- `updateWarehouseItemQuantity(id, operation, amount)` - Обновить количество
- `createWarehouseItem(item)` - Создать новую позицию
- `getWarehouseStats()` - Получить статистику склада

### ProjectsApi
- `getAllProjects()` - Получить все проекты
- `getProjectById(id)` - Получить проект по ID
- `createProject(project)` - Создать новый проект
- `updateProject(id, updates)` - Обновить проект
- `getProjectStats()` - Получить статистику проектов

### ReportsApi
- `getAllReports()` - Получить все отчеты
- `getReportsByProject(projectId)` - Получить отчеты по проекту
- `createReport(report)` - Создать новый отчет
- `approveReport(id, approvedBy)` - Одобрить отчет
- `getReportStats()` - Получить статистику отчетов

### ScheduleApi
- `getAllScheduleEvents()` - Получить все события
- `getTodayScheduleEvents()` - Получить события на сегодня
- `createScheduleEvent(event)` - Создать новое событие
- `updateScheduleEvent(id, updates)` - Обновить событие
- `getScheduleStats()` - Получить статистику расписания

## 🎯 Следующие шаги

1. **Выполните SQL скрипт** в Supabase Dashboard
2. **Обновите компоненты** для использования новых API
3. **Протестируйте функциональность** с реальными данными
4. **Добавьте обработку ошибок** для случаев недоступности API
5. **Настройте уведомления** для пользователей

## ⚠️ Важные замечания

- Все API функции включают обработку ошибок
- При ошибке подключения к Supabase приложение может упасть
- Рекомендуется добавить fallback на моковые данные
- Убедитесь, что RLS политики настроены правильно
- Тестовые данные будут добавлены автоматически

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте подключение к Supabase
2. Убедитесь, что все таблицы созданы
3. Проверьте RLS политики
4. Посмотрите логи в консоли браузера
5. Проверьте Network tab в DevTools

---

**Готово!** Теперь ваше приложение "Отвёртка" готово к работе с реальными данными из базы данных Supabase! 🎉
