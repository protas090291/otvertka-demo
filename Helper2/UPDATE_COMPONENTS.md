# 🔄 Обновление компонентов для использования API

## ✅ Созданы новые компоненты с API:

1. **`BudgetViewWithAPI.tsx`** - Бюджет с интеграцией API
2. **`MaterialsViewWithAPI.tsx`** - Материалы/склад с интеграцией API

## 🔧 Как обновить приложение:

### 1. Замените импорты в App.tsx:

```typescript
// Старые импорты
import BudgetView from './components/BudgetView';
import MaterialsView from './components/MaterialsView';

// Новые импорты
import BudgetViewWithAPI from './components/BudgetViewWithAPI';
import MaterialsViewWithAPI from './components/MaterialsViewWithAPI';
```

### 2. Замените компоненты в JSX:

```typescript
// Старые компоненты
<BudgetView userRole={userRole} />
<MaterialsView userRole={userRole} />

// Новые компоненты
<BudgetViewWithAPI userRole={userRole} />
<MaterialsViewWithAPI userRole={userRole} />
```

## 🎯 Что изменилось:

### BudgetViewWithAPI:
- ✅ Загружает данные из Supabase через API
- ✅ Добавляет новые бюджетные операции в базу данных
- ✅ Удаляет операции из базы данных
- ✅ Показывает индикатор загрузки
- ✅ Обрабатывает ошибки

### MaterialsViewWithAPI:
- ✅ Загружает складские позиции из Supabase
- ✅ Добавляет новые позиции в базу данных
- ✅ Обновляет существующие позиции
- ✅ Удаляет позиции из базы данных
- ✅ Показывает индикатор загрузки
- ✅ Обрабатывает ошибки

## 🚀 Результат:

После обновления компоненты будут:
- Работать с реальными данными из базы данных
- Сохранять изменения в Supabase
- Синхронизироваться между пользователями
- Не терять данные при перезагрузке

## 📝 Следующие шаги:

1. Обновите App.tsx с новыми импортами
2. Протестируйте функциональность
3. Создайте аналогичные компоненты для:
   - ReportsView
   - ScheduleView
   - ProjectsView

## ⚠️ Важно:

- Старые компоненты остаются для резервного копирования
- Новые компоненты используют те же пропсы
- Интерфейс пользователя не изменился
- Все функции работают как раньше, но с реальными данными
