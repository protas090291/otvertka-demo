# ✅ Исправление проблемы с кодировкой кириллицы

## Проблема

При открытии файлов с кириллицей в имени возникала ошибка:
```
'latin-1' codec can't encode character '\u0422' in position 18: ordinal not in range(256)
```

**Причина:** Заголовок HTTP `Content-Disposition` по умолчанию использует кодировку latin-1, которая не поддерживает кириллицу.

## Решение

Использован стандарт **RFC 2231** для кодирования имен файлов с не-ASCII символами.

### Изменения в коде

**Файл:** `Helper2/backend/simple_main.py`

#### 1. Функция `view_yandex_disk_file` (строки 524-535)

**Было:**
```python
'Content-Disposition': f'inline; filename="{file_name}"'
```

**Стало:**
```python
# Правильно кодируем имя файла для заголовка Content-Disposition (RFC 2231)
from urllib.parse import quote
try:
    file_name.encode('ascii')
    # Имя файла содержит только ASCII символы
    content_disposition = f'inline; filename="{file_name}"'
except UnicodeEncodeError:
    # Имя файла содержит не-ASCII символы (кириллица и т.д.)
    # Используем RFC 2231 формат: filename*=UTF-8''encoded_name
    encoded_name = quote(file_name, safe='')
    content_disposition = f"inline; filename*=UTF-8''{encoded_name}"
```

#### 2. Функция `download_yandex_disk_file` (строки 452-462)

**Было:**
```python
'Content-Disposition': f'attachment; filename="{file_name}"'
```

**Стало:**
```python
# Правильно кодируем имя файла для заголовка Content-Disposition (RFC 2231)
from urllib.parse import quote
try:
    file_name.encode('ascii')
    content_disposition = f'attachment; filename="{file_name}"'
except UnicodeEncodeError:
    encoded_name = quote(file_name, safe='')
    content_disposition = f"attachment; filename*=UTF-8''{encoded_name}"
```

## Как это работает

1. **Проверка кодировки:**
   - Пытаемся закодировать имя файла в ASCII
   - Если успешно - используем обычный формат
   - Если ошибка `UnicodeEncodeError` - используем RFC 2231

2. **RFC 2231 формат:**
   - Формат: `filename*=UTF-8''encoded_name`
   - `UTF-8` - кодировка
   - `encoded_name` - URL-encoded имя файла
   - Поддерживается всеми современными браузерами

3. **Примеры:**
   - `file.pdf` → `filename="file.pdf"` (ASCII)
   - `Т1202_AM.pdf` → `filename*=UTF-8''%D0%A21202_AM.pdf` (кириллица)

## Результат

✅ **Исправлено:**
- Файлы с кириллицей в имени открываются без ошибок
- Файлы с кириллицей скачиваются с правильным именем
- Поддерживаются любые Unicode символы в именах файлов

✅ **Backend перезапущен:**
- Изменения применены
- Endpoints работают корректно

## Проверка

После перезапуска backend:

1. Откройте приложение: http://localhost:5175
2. Перейдите в раздел "Документы из Яндекс Диска"
3. Найдите файл с кириллицей в имени (например, `Т1202_AM.pdf`)
4. Нажмите кнопку **"Открыть"**
5. Файл должен открыться без ошибок кодировки

---

**Проблема исправлена! Файлы с кириллицей теперь открываются корректно!** 🚀









