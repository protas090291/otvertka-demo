# ✅ Проверка настройки .env файла

## 🔍 Проблема

Ошибка: `YANDEX_DISK_TOKEN не установлен в переменных окружения`

## ✅ Решение

Я добавил загрузку `.env` файла в `simple_main.py`. Теперь нужно:

### 1. Создать файл `.env`

Создайте файл `Helper2/backend/.env` со следующим содержимым:

```env
YANDEX_DISK_TOKEN=y0__xDplMyRCBiSuzsgrfjujhWQozV343SXWgSk3mhHwhrrnSQt5g
YANDEX_DISK_FOLDER_PATH=/Вишневый сад-3 для Заказчика
```

### 2. Проверить расположение файла

Файл `.env` должен находиться в папке:
```
Helper2/backend/.env
```

**Важно:**
- ✅ Файл должен называться именно `.env` (с точкой в начале)
- ✅ Файл должен быть в папке `backend/`
- ✅ Не должно быть расширения (не `.env.txt`)

### 3. Перезапустить backend сервер

После создания файла `.env`:

1. **Остановите backend сервер** (если запущен)
   - Нажмите `Ctrl+C` в терминале, где запущен backend

2. **Запустите backend сервер заново:**
   ```bash
   cd Helper2/backend
   python simple_main.py
   ```

3. **Проверьте логи:**
   - Не должно быть ошибок о `.env` файле
   - Сервер должен запуститься успешно

### 4. Проверить работу

1. Откройте приложение: http://localhost:5175
2. Перейдите в раздел "Документы из Яндекс Диска"
3. Файлы должны загрузиться!

## 🔍 Проверка файла .env

### Способ 1: Через Python

Выполните в терминале:

```bash
cd Helper2/backend
python -c "from dotenv import load_dotenv; import os; load_dotenv(); print('Токен:', 'установлен' if os.getenv('YANDEX_DISK_TOKEN') else 'НЕ установлен'); print('Путь:', os.getenv('YANDEX_DISK_FOLDER_PATH', 'НЕ установлен'))"
```

**Ожидаемый результат:**
```
Токен: установлен
Путь: /Вишневый сад-3 для Заказчика
```

### Способ 2: Проверка файла вручную

1. Откройте папку `Helper2/backend/`
2. Убедитесь, что файл `.env` существует
3. Откройте файл и проверьте содержимое

## ⚠️ Частые ошибки

### Ошибка: "Файл .env не найден"

**Решение:**
- Убедитесь, что файл создан в правильной папке: `Helper2/backend/.env`
- Проверьте, что файл называется именно `.env` (не `.env.txt`)

### Ошибка: "Токен не установлен"

**Решение:**
- Проверьте, что в файле `.env` есть строка: `YANDEX_DISK_TOKEN=...`
- Убедитесь, что после `=` указан токен
- Проверьте, что нет лишних пробелов вокруг `=`

### Ошибка: "Путь не установлен"

**Решение:**
- Проверьте, что в файле `.env` есть строка: `YANDEX_DISK_FOLDER_PATH=...`
- Убедитесь, что путь указан правильно

## 📝 Пример правильного файла .env

```env
YANDEX_DISK_TOKEN=y0__xDplMyRCBiSuzsgrfjujhWQozV343SXWgSk3mhHwhrrnSQt5g
YANDEX_DISK_FOLDER_PATH=/Вишневый сад-3 для Заказчика
```

**Важно:**
- ✅ Нет пробелов вокруг `=`
- ✅ Нет кавычек вокруг значений
- ✅ Каждая переменная на отдельной строке
- ✅ Путь начинается с `/`

## ✅ После исправления

1. Создайте файл `.env` с правильным содержимым
2. Перезапустите backend сервер
3. Проверьте работу в приложении

---

**Готово!** После создания файла `.env` и перезапуска сервера все должно работать.









