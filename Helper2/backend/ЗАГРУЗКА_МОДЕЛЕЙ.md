# 📥 Предзагрузка PaddleOCR-VL моделей

## ✅ Статус

**Загрузка запущена!** Процесс работает в фоновом режиме.

## 🎯 Что происходит сейчас

Модели PaddleOCR-VL загружаются автоматически:
- **Размер:** ~1-2 ГБ
- **Время:** 5-15 минут (зависит от скорости интернета)
- **Процесс:** автоматический, не требует вмешательства

## 📋 Проверка статуса

### Способ 1: Через скрипт проверки
```bash
python3 Helper2/backend/check_preload_status.py
```

### Способ 2: Через Python
```python
from pdf_ai_processor import _processor_instance
if _processor_instance is not None:
    print("✅ Модель загружена!")
else:
    print("⏳ Модель еще загружается...")
```

### Способ 3: Через API
```bash
curl http://localhost:8000/api/pdf/health
```

## 🚀 Ручной запуск загрузки

Если нужно запустить загрузку вручную:

```bash
cd Helper2/backend
python3 quick_preload.py
```

Или в фоновом режиме:

```bash
cd Helper2/backend
nohup python3 quick_preload.py > preload.log 2>&1 &
```

## ✅ После загрузки

Когда модель загрузится, вы сможете использовать API для:
- ✅ Извлечения текста из PDF
- ✅ Извлечения таблиц
- ✅ Распознавания формул
- ✅ Распознавания графиков
- ✅ OCR (распознавание текста из изображений)

## 📝 Примечания

- **Первый запуск:** 5-15 минут (загрузка моделей)
- **Последующие запуски:** мгновенно (модели уже загружены)
- **Автоматическая загрузка:** Модели загрузятся автоматически при первом использовании API, если не были загружены заранее

## 🎉 Готово!

После завершения загрузки модель будет готова к быстрому использованию!





## ✅ Статус

**Загрузка запущена!** Процесс работает в фоновом режиме.

## 🎯 Что происходит сейчас

Модели PaddleOCR-VL загружаются автоматически:
- **Размер:** ~1-2 ГБ
- **Время:** 5-15 минут (зависит от скорости интернета)
- **Процесс:** автоматический, не требует вмешательства

## 📋 Проверка статуса

### Способ 1: Через скрипт проверки
```bash
python3 Helper2/backend/check_preload_status.py
```

### Способ 2: Через Python
```python
from pdf_ai_processor import _processor_instance
if _processor_instance is not None:
    print("✅ Модель загружена!")
else:
    print("⏳ Модель еще загружается...")
```

### Способ 3: Через API
```bash
curl http://localhost:8000/api/pdf/health
```

## 🚀 Ручной запуск загрузки

Если нужно запустить загрузку вручную:

```bash
cd Helper2/backend
python3 quick_preload.py
```

Или в фоновом режиме:

```bash
cd Helper2/backend
nohup python3 quick_preload.py > preload.log 2>&1 &
```

## ✅ После загрузки

Когда модель загрузится, вы сможете использовать API для:
- ✅ Извлечения текста из PDF
- ✅ Извлечения таблиц
- ✅ Распознавания формул
- ✅ Распознавания графиков
- ✅ OCR (распознавание текста из изображений)

## 📝 Примечания

- **Первый запуск:** 5-15 минут (загрузка моделей)
- **Последующие запуски:** мгновенно (модели уже загружены)
- **Автоматическая загрузка:** Модели загрузятся автоматически при первом использовании API, если не были загружены заранее

## 🎉 Готово!

После завершения загрузки модель будет готова к быстрому использованию!







