# 📥 Инструкция по предзагрузке PaddleOCR-VL

## 🎯 Зачем нужна предзагрузка?

При первом использовании PaddleOCR-VL загружает модели (~1-2 ГБ), что занимает 5-15 минут. 
Предзагрузка позволяет загрузить модели заранее, чтобы потом использовать API быстро.

## 🚀 Способы предзагрузки:

### Способ 1: Автоматическая загрузка (рекомендуется)

Просто запустите скрипт предзагрузки:

```bash
cd Helper2/backend
python3 quick_preload.py
```

Или в фоновом режиме:

```bash
cd Helper2/backend
nohup python3 quick_preload.py > preload.log 2>&1 &
```

### Способ 2: Проверка статуса

Проверить, загружена ли модель:

```bash
cd Helper2/backend
python3 check_preload_status.py
```

### Способ 3: Через API (автоматически)

При первом запросе к API модель загрузится автоматически:

```bash
curl http://localhost:8000/api/pdf/health
```

## ⏱️ Время загрузки:

- **Первый раз:** 5-15 минут (зависит от скорости интернета)
- **Последующие запуски:** мгновенно (модели уже загружены)

## ✅ Проверка готовности:

После загрузки вы увидите:
```
✅ PaddleOCR-VL успешно загружен!
✅ Модели готовы к использованию
```

## 📝 Логи:

Логи загрузки сохраняются в:
- `Helper2/backend/preload.log` (если запущено в фоне)
- Консоль (если запущено напрямую)

## 🎉 После загрузки:

Модель готова к использованию! Можно использовать API для:
- Извлечения текста из PDF
- Извлечения таблиц
- Распознавания формул
- Распознавания графиков
- OCR (распознавание текста из изображений)





