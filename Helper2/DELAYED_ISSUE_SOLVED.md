# 🎯 ПРОБЛЕМА С КОЛОНКОЙ "ПРОСРОЧЕНО" РЕШЕНА!

## 🔍 **Найденные проблемы:**

### ❌ **Проблема 1: CHECK constraint**
```
new row for relation "tasks" violates check constraint "tasks_status_check"
```
**Причина:** В базе данных CHECK constraint не разрешает статус "delayed"

### ❌ **Проблема 2: Неправильное имя колонки**
```
Could not find the 'order_index' column of 'tasks' in the schema cache
```
**Причина:** В коде используется `order_index`, а в БД колонка называется `order`

## ✅ **Исправления:**

### 🔧 **1. Исправлен CHECK constraint**
```sql
-- Удаляем старый constraint
ALTER TABLE tasks DROP CONSTRAINT IF EXISTS tasks_status_check;

-- Создаем новый с поддержкой "delayed"
ALTER TABLE tasks ADD CONSTRAINT tasks_status_check 
CHECK (status IN ('pending', 'in_progress', 'completed', 'delayed'));
```

### 🔧 **2. Исправлено имя колонки в коде**
```typescript
// БЫЛО:
.update({ 
  order_index: order,
  status 
})

// СТАЛО:
.update({ 
  order: order,
  status 
})
```

## 📁 **Измененные файлы:**
- `Helper2/src/lib/tasksApi.ts` - исправлено `order_index` → `order`
- `Helper2/FIX_DELAYED_AND_ORDER.sql` - SQL скрипт для исправления БД

## 🧪 **Тестирование:**

### Шаг 1: Выполните SQL скрипт
```sql
-- Выполните в Supabase SQL Editor:
-- FIX_DELAYED_AND_ORDER.sql
```

### Шаг 2: Проверьте приложение
1. Обновите страницу (Ctrl+F5)
2. Перейдите в режим "Канбан"
3. Попробуйте перетащить задачу в "Просрочено"

### Шаг 3: Ожидаемый результат
- ✅ Задачи должны перетаскиваться в "Просрочено"
- ✅ Статус должен сохраняться в базе данных
- ✅ Анимации должны работать
- ✅ В консоли не должно быть ошибок

## 🎉 **Результат:**
После выполнения исправлений колонка "Просрочено" должна работать полностью!

## 📋 **Логи успешного перетаскивания:**
```
Dropped on column: delayed
✅ API: Статус задачи успешно обновлен
✅ API: Порядок задачи успешно обновлен
```

## ⚠️ **Если проблемы остаются:**
1. Убедитесь, что SQL скрипт выполнен полностью
2. Проверьте, что в Supabase нет ошибок
3. Обновите страницу приложения
4. Проверьте консоль браузера на новые ошибки




















