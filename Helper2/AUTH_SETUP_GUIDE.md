# 🔐 Руководство по настройке аутентификации и модульной системы

## 📋 Что было реализовано

1. ✅ **Аутентификация через Supabase Auth** (Email/Password)
2. ✅ **Модульная система** с экраном выбора модулей
3. ✅ **Управление пользователями** для админов
4. ✅ **Роли и доступы**: `admin`, `management`, `user`

## 🚀 Пошаговая настройка

### Шаг 1: Настройка Supabase Auth

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект: `yytqmdanfcwfqfqruvta`
3. Перейдите в **Authentication** → **Providers**
4. Убедитесь, что **Email** провайдер включён (по умолчанию включён)

### Шаг 2: Создание таблицы user_profiles

1. Перейдите в **SQL Editor**
2. Создайте новый запрос
3. Скопируйте и выполните содержимое файла **`supabase-user-profiles.sql`**

Это создаст:
- Таблицу `user_profiles` для расширенных данных пользователей
- Триггеры для автоматического создания профиля при регистрации
- RLS политики для безопасности
- Индексы для быстрой работы

### Шаг 3: Создание первого администратора

После выполнения SQL скрипта создайте первого админа:

1. Перейдите в **Authentication** → **Users**
2. Нажмите **"Add user"** → **"Create new user"**
3. Заполните:
   - **Email**: например, `admin@company.ru`
   - **Password**: придумайте пароль (минимум 6 символов)
   - **Auto Confirm User**: включите ✅
4. Нажмите **"Create user"**

5. После создания пользователя откройте **SQL Editor** и выполните:

```sql
-- Замените 'admin@company.ru' на email созданного пользователя
UPDATE user_profiles 
SET role = 'admin' 
WHERE email = 'admin@company.ru';
```

### Шаг 4: Проверка работы

1. Запустите приложение: `npm run dev`
2. Войдите с учётными данными админа
3. Вы должны увидеть экран выбора модулей с 3 карточками:
   - Рабочий состав
   - Руководство
   - Администрирование

## 📊 Структура ролей и доступов

| Роль | Доступные модули | Описание |
|------|------------------|-----------|
| **user** (Рабочий) | Только "Рабочий состав" | Обычные рабочие и исполнители |
| **management** (Руководство) | "Рабочий состав" + "Руководство" | Руководители проектов |
| **admin** (Администратор) | Все 3 модуля | Полный доступ ко всему |

## 🎯 Логика работы

### После входа:
1. Пользователь входит через Supabase Auth
2. Система проверяет роль пользователя
3. Если доступен только 1 модуль → открывается сразу
4. Если доступно несколько модулей → показывается экран выбора модулей

### Кнопка "Выйти":
- **Рабочий** (`user`): полный выход из аккаунта (logout)
- **Руководство/Админ**: возврат к экрану выбора модулей (без logout)

### Сохранение модуля:
- При выборе модуля он сохраняется в `localStorage`
- При следующем входе открывается последний выбранный модуль (если доступ разрешён)

## 👥 Управление пользователями (для админов)

В модуле **"Администрирование"** админ может:

- ✅ **Создавать пользователей** (email, пароль, имя, роль)
- ✅ **Редактировать пользователей** (имя, роль)
- ✅ **Активировать/деактивировать** пользователей
- ✅ **Удалять пользователей**
- ✅ **Просматривать список** всех пользователей

## 🔧 API функции (authApi.ts)

- `signIn(credentials)` - вход в систему
- `signOut()` - выход из системы
- `getCurrentUser()` - получить текущего пользователя
- `getAllUserProfiles()` - получить всех пользователей (только для админов)
- `createUser(userData)` - создать пользователя (только для админов)
- `updateUserProfile(userId, updates)` - обновить профиль (только для админов)
- `deleteUser(userId)` - удалить пользователя (только для админов)
- `getAvailableModules(user)` - получить доступные модули для пользователя

## ⚠️ Важные замечания

1. **Первый админ**: После создания таблицы `user_profiles` создайте первого админа вручную через SQL (см. Шаг 3)

2. **RLS политики**: Таблица защищена Row Level Security - только админы могут создавать/редактировать пользователей

3. **Автоматическое создание профиля**: При создании пользователя через Supabase Auth автоматически создаётся запись в `user_profiles` (через триггер)

4. **Email подтверждение**: В настройках Supabase Auth можно отключить требование подтверждения email для упрощения тестирования

## 🐛 Решение проблем

### Пользователь не может войти:
- Проверьте, что Email провайдер включён в Supabase
- Проверьте, что пользователь существует в `auth.users`
- Проверьте, что профиль создан в `user_profiles`
- Проверьте, что `is_active = true` в профиле

### Админ не видит модуль "Администрирование":
- Проверьте, что `role = 'admin'` в таблице `user_profiles`
- Обновите страницу (F5)

### Ошибка "Профиль пользователя не найден":
- Проверьте, что триггер `on_auth_user_created` создан и работает
- Создайте профиль вручную через SQL если нужно

## ✅ Готово!

После выполнения всех шагов система аутентификации и модульная система будут работать полностью.
