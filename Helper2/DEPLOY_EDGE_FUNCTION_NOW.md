# 🚀 Быстрый деплой Edge Function

## Шаг 1: Установка Supabase CLI

Выберите один из способов:

### Вариант А: Через npm (требует sudo)

```bash
sudo npm install -g supabase
```

### Вариант Б: Через Homebrew (рекомендуется для macOS)

```bash
brew install supabase/tap/supabase
```

### Вариант В: Через npx (без установки)

Можно использовать без установки через `npx`, но для деплоя лучше установить.

## Шаг 2: Вход в Supabase

```bash
supabase login
```

Откроется браузер для авторизации. Войдите в свой аккаунт Supabase.

## Шаг 3: Переход в папку проекта

```bash
cd "/Users/zaharprotas/Desktop/OwnerReady — 2.0/Helper2"
```

Или откройте терминал прямо в папке `Helper2`.

## Шаг 4: Связывание проекта

```bash
supabase link --project-ref yytqmdanfcwfqfqruvta
```

Если спросит пароль базы данных - можете пропустить (нажмите Enter), он не нужен для Edge Functions.

## Шаг 5: Деплой функции

```bash
supabase functions deploy import-estimate
```

## Шаг 6: Проверка

После успешного деплоя вы увидите:
```
Deploying function import-estimate...
Function import-estimate deployed successfully!
```

## Готово! ✅

Теперь Edge Function работает и будет обрабатывать все импорты сметы автоматически.

## Если возникли проблемы

### Ошибка: "Not logged in"
```bash
supabase login
```

### Ошибка: "Project not found"
Проверьте project-ref в Supabase Dashboard:
- Settings → General → Reference ID

### Ошибка: "Function not found"
Убедитесь, что вы в правильной папке:
```bash
ls supabase/functions/import-estimate/
```
Должен быть файл `index.ts`

## Проверка логов

```bash
supabase functions logs import-estimate --follow
```
