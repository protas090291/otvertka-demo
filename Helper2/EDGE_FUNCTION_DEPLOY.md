# 🚀 Инструкция по деплою Edge Function для импорта сметы

## Что было сделано

1. ✅ Создана Edge Function `import-estimate` в `supabase/functions/import-estimate/`
2. ✅ Логика парсинга Excel/CSV перенесена на сервер
3. ✅ Фронтенд обновлен для использования Edge Function (с fallback на клиентский парсинг)

## Преимущества

- ⚡ **Быстрее**: Парсинг на сервере, браузер не блокируется
- 🔒 **Надежнее**: Валидация и обработка ошибок на сервере
- 📈 **Масштабируемо**: Файлы любого размера
- 🎯 **Качественно**: Та же логика парсинга, что и раньше

## Шаг 1: Установка Supabase CLI

```bash
# Установите Supabase CLI глобально
npm install -g supabase

# Или через Homebrew (macOS)
brew install supabase/tap/supabase
```

## Шаг 2: Вход в Supabase

```bash
supabase login
```

Откроется браузер для авторизации.

## Шаг 3: Связывание проекта

```bash
# Перейдите в папку проекта
cd Helper2

# Свяжите проект с вашим Supabase проектом
supabase link --project-ref yytqmdanfcwfqfqruvta
```

**Где найти project-ref:**
- Откройте Supabase Dashboard
- Выберите ваш проект
- Settings → General → Reference ID

## Шаг 4: Деплой Edge Function

```bash
# Деплой функции
supabase functions deploy import-estimate

# Или с указанием проекта явно
supabase functions deploy import-estimate --project-ref yytqmdanfcwfqfqruvta
```

## Шаг 5: Проверка деплоя

После успешного деплоя вы увидите:
```
Deploying function import-estimate...
Function import-estimate deployed successfully!
```

## Шаг 6: Тестирование

1. Откройте приложение
2. Перейдите в раздел "Смета"
3. Нажмите "Импорт Excel/CSV"
4. Выберите файл сметы
5. Проверьте, что импорт работает через Edge Function

## Проверка логов

```bash
# Просмотр логов Edge Function
supabase functions logs import-estimate

# В реальном времени
supabase functions logs import-estimate --follow
```

## Откат к клиентскому парсингу

Если Edge Function не работает, фронтенд автоматически использует старую логику парсинга на клиенте (fallback).

## Устранение проблем

### Ошибка: "Function not found"
- Убедитесь, что функция задеплоена: `supabase functions list`
- Проверьте, что проект правильно связан: `supabase projects list`

### Ошибка: "Permission denied"
- Проверьте, что вы авторизованы: `supabase login`
- Убедитесь, что у вас есть права на проект

### Ошибка парсинга Excel
- Проверьте формат файла (должен быть .xlsx, .xls или .csv)
- Проверьте логи: `supabase functions logs import-estimate`

## Структура файлов

```
Helper2/
├── supabase/
│   └── functions/
│       └── import-estimate/
│           ├── index.ts          # Основной код функции
│           └── README.md         # Документация
└── src/
    ├── lib/
    │   └── estimateApi.ts        # Обновлен: добавлен importEstimateFromFile
    └── components/
        └── EstimateView.tsx      # Обновлен: использует Edge Function
```

## Что дальше?

После успешного деплоя:
1. ✅ Edge Function будет обрабатывать все импорты сметы
2. ✅ Фронтенд станет проще и быстрее
3. ✅ Можно обрабатывать файлы любого размера

## Дополнительная информация

- [Документация Supabase Edge Functions](https://supabase.com/docs/guides/functions)
- [Supabase CLI Reference](https://supabase.com/docs/reference/cli)
